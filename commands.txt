
# for screen recorder short cut
Ctrl + Alt + Shift + R

1. update kube config 

aws eks update-kubeconfig --region ap-south-1 --name flask-cluster

2. helm section

# 1. Ingress controller
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

# 2. Now install
helm install ingress-nginx ingress-nginx/ingress-nginx \
  --namespace ingress-nginx --create-namespace

kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml

helm install prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring --create-namespace

 helm uninstall prometheus --namespace monitoring
 kubectl delete namespace monitoring
 helm repo remove prometheus-community




 #scp command 
 sudo scp  -i Downloads/wordpress.pem -r /home/andrews/Documents/andrews/icanio-devops/k8s  ubuntu@13.207.213.172:/home/ubuntu 


 # to get grafana admin password
 kubectl get secret --namespace monitoring prometheus-grafana -o jsonpath="{.data.admin-password}" | base64 --decode; echo



 # port forward to  prometheus dashboard from eks cluster to local machine (once run this command it will hang)
 ssh -i Downloads/wordpress.pem -N -L 3000:localhost:3000 -L 9090:localhost:9090 ubuntu@13.235.149.20  # run in local machine terminal

 kubectl port-forward svc/prometheus-kube-prometheus-prometheus -n monitoring 9090:9090  # run in cloud machine terminal


 # port forward to  prometheus dashboard from eks cluster to local machine (once run this command it will hang)
 ssh -i Downloads/wordpress.pem -N -L 3000:localhost:3000 -L 9090:localhost:9090 ubuntu@13.235.149.20  # run in local machine terminal

 kubectl port-forward svc/prometheus-kube-prometheus-prometheus -n monitoring 9090:9090  # run in cloud machine terminal

 # password for grafana dashboard

 Username: admin

 Password: prom-operator (This is the standard default for the kube-prometheus-stack helm chart).


 # example command for load with wrk  for flask app
 wrk -t12 -c400 -d120s http://a58361c38614642b387677eb8eb2f490-197803309.ap-south-1.elb.amazonaws.com:5000/
 


 